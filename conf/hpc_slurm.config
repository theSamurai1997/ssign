/*
 * SLURM scheduler profile.
 *
 * Uses Singularity for containers.
 */

singularity {
    enabled    = true
    autoMounts = true
}

docker {
    enabled = false
}

process {
    executor = 'slurm'

    withLabel: 'process_gpu' {
        clusterOptions = '--gres=gpu:1'
    }
}
