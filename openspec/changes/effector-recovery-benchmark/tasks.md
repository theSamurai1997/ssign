## 1. Setup and teardown

- [x] 1.1 Remove the prior broken benchmark: delete `validation_sweeps/analysis/05_*`-`11_*` scripts and figures `09`/`10`/`11`; confirm figures `01`-`04`, `07` are untouched.
- [x] 1.2 Create the benchmark workspace: `validation_sweeps/benchmark/` with `data/` (gold set + answer key), `scripts/`, `figures/`, and a `README.md` stub.
- [x] 1.3 Copy the source effector corpus (`secretion-classifier/data/verified/T*_verified.tsv`) into `validation_sweeps/benchmark/data/source_corpus/` so the benchmark is self-contained; record the source commit hash.

## 2. Phase 0a: effector gold set

- [x] 2.1 Filter to `evidence_level = validated`; write `validated_raw.tsv` with per-type counts logged.
- [x] 2.2 Partition by audit status: VERIFIED (keep), PARTIAL (repair queue), FAIL/NEEDS_REVIEW (review queue); write each subset.
- [x] 2.3 Drop the known biology errors (apparatus-as-effector Hcp/VgrG/PAAR, immunity/adaptor) by locus-aware rule, 38 dropped + 12 FAIL/review; record each with reason. (5 "real effector, wrong metadata" rows held for user decision: see data/gold_build/PENDING_DECISIONS.md.)
- [x] 2.4 Rebuild `uniprot` accessions from `locus_tag` via UniProt REST for the repair queue; flag any locus_tag that fails to map. (Audit-faithful: replace only audit-flagged accessions, fill empties, cross-check the rest. 67 corrected, 67 filled, 18 confirmed, 1 conflict, 7 unresolved-flagged. See `scripts/02_repair_gold_set.py`, `data/gold_build/04_repaired.tsv`.)
- [x] 2.5 Re-cite broken DOIs for the repair queue (5-row pilot + 54-row Workflow + 4 filter-blocked done by hand), each with a verbatim secretion quote. 59 broken DOIs: 51 RESOLVED, 8 CONTRADICTED. Records in `data/gold_build/doi_repair_{pilot,workflow,manual}.jsonl`. Adjudications (Teo 2026-06-10): drop celY/pemB/paeX/CagF/Sca4/Lem4; reclassify TRP47+TRP32 to T1SS; keep TcpB/Btp1 x2 as T4SS.
- [x] 2.6 Independent recheck: Crossref re-confirms every gold-set DOI resolves (DOI.org/handle and PMC-converter both unreliable here). 212/247 resolve; 35 are legacy-format Science/PNAS DOIs in audit-VERIFIED rows (false-negatives, trust the audit). DEFERRED: (a) 2 REPAIRED rows (xynA, pglA) cite a non-resolving 2020 review DOI - re-repair; (b) optional agent re-verify of the 35 legacy DOIs for max rigor. locus_tag-resolves-in-genome check folded into Phase 1 gene-order build (5.1).
- [x] 2.7 Emit `gold_set_corpus.tsv` (523 effectors: T1=28 T2=83 T3=237 T4=105 T6=70) + `gold_set_provenance.tsv` (every row's fate). 349 verified-clean + 172 repaired + 2 reclassified; 6 contradicted + 3 unplaceable dropped. See `scripts/03_finalize_gold_set.py`.

## 2b. Phase 0a: external-database reconciliation

- [x] 2b.1 Acquired SecReT4 (`verified_effectors.fas`, 540 verified T4SS) + SecReT6 (`effector_exp_protein.fasta`, 331 exp T6SS) from the SJTU mirror; per-effector metadata scraped from SecReT6 record pages (`scripts/05_scrape_secret6.py`). EffectiveDB excluded (prediction-only, no per-effector citations). BastionHub DOWN (DNS) + Wayback 429 -> T1/T2SS net-new is a recorded coverage gap. See `data/external_dbs/MANIFEST.md`.
- [x] 2b.2 Parsed both DBs to a common schema (`scripts/04_parse_external_dbs.py` -> `parsed_external.tsv`); experimental/verified tiers only. SecReT4 accessions resolved to locus_tag via NCBI (`scripts/06_map_secret4.py`).
- [x] 2b.3 Dedup vs `gold_set_corpus.tsv` on locus_tag (universal key) + UniProt accession (`scripts/07_reconcile.py`). Net-new split by detection gate (Teo: gate on detected systems) and panel status (exact-genome match): SecReT4 = 59 dup, **289 GATED** (Dot/Icm type-IVB, held pending Phase 2), 40 type-IVA needs-citation. SecReT6 = 29 dup, **55 in-panel** + 219 panel-expansion + 16 needs-citation. Outputs: `net_new_{active,panel_expansion,gated,needs_citation}.tsv`.
- [x] 2b.4 Bar = experimental support (DB experimental tier) + resolvable citation + locus resolves (folded into Phase 1, like 2.6). 14 in-panel needs-citation resolved via PubMed (`data/external_dbs/citation_overrides.tsv`): **6 SecReT4 type-IVA folded** with verified DOIs (VceC/VceA = de Jong 2008 10.1111/j.1365-2958.2008.06487.x; BAB1_1275/BAB1_2005 = Marchesini 2011 10.1111/j.1462-5822.2011.01618.x; VirD5/Atu6185 = Vergunst 2005 10.1073/pnas.0406241102; VirE3/Atu6191 = Padavannil 2014 10.1371/journal.ppat.1003948); **5 SecReT6 Serratia bare apparatus dropped** (Hcp x3, VgrG x2 - Phase 0a rule); **3 held no-citation** (BMEI0736 possible SecReT4 mislabel = NCBI carbonic anhydrase, not in B.abortus screens; Ssp4/Ssp5 = SecReT6 pages carry no DOI). Also re-applied the Phase 0a apparatus drop to the whole net-new set: caught 2 bare apparatus (hcp2 PA0263, PAAR2 PA0824) that the fold had silently re-introduced -> `net_new_apparatus_dropped.tsv` (53 T6SS). panel-expansion (177) + GATED (289) verified later only if brought in-scope at Checkpoint A.
- [x] 2b.5 Emitted `effector_gold_set.tsv` (523 corpus + 59 in-panel net-new = **582**) + `reconciliation_report.tsv`. Per-type after fold: T1 28 / T2 83 / T3 237 / T4 **111** / T6 **123**. (T6SS 70->123 after dropping the 2 re-introduced apparatus; T4SS 105->111 after folding the 6 type-IVA.)

## 3. Phase 0b: machinery answer key

- [x] 3.1 Enumerated distinct instances (ss_type, refseq_genome, sys_instance_id) from gold_set_corpus.tsv -> `data/machinery/instances.tsv` = **90 instances / 61 genomes** (T1 8 / T2 16 / T3 32 / T4 13 / T6 21). `scripts/08_enumerate_instances.py`. 24 effectors unplaceable (no genome / "-" placeholder, 19 are T1SS) logged to `unplaceable_effectors.tsv`. Net-new external rows excluded (no sys_instance_id; assigned in Phase 1).
- [x] 3.2 Fetched RefSeq/INSDC GenBank (gbwithparts) for all 61 instance genomes -> `data/refseq_cache/<acc>.gb` (476 MB). `scripts/09_fetch_refseq.py`, cached/resumable.
- [x] 3.3 Wrote 5 apparatus briefs (T1/T2/T3/T4/T6) + README -> `scripts/apparatus_briefs/`. Canonical gene families + system synonyms; T6SS brief flags Hcp/VgrG/PAAR as machinery (not effectors here).
- [x] 3.4 **All 90 instances curated** -> `data/machinery_raw/<id>.json` (literature-only; gene+family+role+DOI+verbatim quote each). Pilot 5/5 verified first, then 2 Workflow rounds (85 then 39 retry; session-limit + spurious AUP blocks). The 4 CDC-select-agent instances (T4SS_03 Coxiella, T4SS_06/T4SS_08 Brucella, T3SS_21 B.pseudomallei) repeatedly AUP-blocked in subagents -> hand-curated in main session via PubMed (founding papers: Sieira 2000, O'Callaghan 1999, Zamboni 2003, Stevens 2002/04). QA integrity pass caught + fixed T6SS_15 (9 genes had been cited to a non-PubMed fisheries review -> re-anchored all 13 to the founding Zheng & Leung 2007, PMID 17986187). Coverage: **69 COMPLETE / 21 PARTIAL**, 1135 machinery genes (T1 34 / T2 197 / T3 521 / T4 133 / T6 250). Full per-type breakdown logged in 3.7.
- [x] 3.5 `scripts/10_resolve_machinery.py` -> `data/machinery/machinery_resolved.tsv` (1135 rows). Alias- and replicon-aware: matches each gene's native symbol (`gene`) AND unified family token (`family`, e.g. EpsC->GspC) case-insensitively against /gene + /gene_synonym + /old_locus_tag + /locus_tag across ALL cached replicons of the instance's organism; secondary /product word-match tier flagged separately for 3.6. Fetched B. melitensis chr II (NC_003318.1) to close the documented cross-replicon virB gap. **Resolved 902/1135 (79%): 772 alias + 130 product-tier; 96 cross-replicon; 175 ambiguous (>1 candidate).** Per type: T1 97% / T3 86% / T2 80% / T4 80% / T6 63%. **86/90 instances anchored (>=1 gene placed).** 4 zero-placed -> 3.6:
  - T2SS_16: instance genome `X12831` is a 1823 bp EMBL *pul*-operon fragment, not a genome -> genome-backfill (task 5.2), reassign Klebsiella genome.
  - T6SS_07 (P. aeruginosa PAO1 H3-T6SS): native Hsi3/Hcp3/VgrG3/ClpV3 names; PAO1 annotates only clpV1, no Tss/Hsi3 in /gene or /product -> needs founding-paper locus_tags.
  - T6SS_11 (A. baumannii 17978) + T6SS_20 (X. citri): genome annotates T6SS genes as hypothetical/locus-tag-only (no "type VI"/Tss/Hcp/VgrG in /gene or /product) -> needs founding-paper locus_tags.
- [x] 3.6 `scripts/11_verify_machinery.py` -> `data/machinery/machinery_answer_key.tsv` (per-gene `verdict` + `doi_resolves`/`pmid_valid`/`quote_match`, per-instance `anchor_status`). Checks: DOI registered (HEAD doi.org no-redirect, cached) + PMID real (batch NCBI esummary) + gene named in the 3.4 quote. **Citations near-perfect: DOI 1134/1135 (the 1 is a legacy false-negative, valid PMID), PMID 1135/1135; quote names gene 955, operon-collective 180.** Per-gene: **772 verified** (alias-tier + coords + cited) / 130 review (product-tier) / 233 unverified (no locus). Per-instance: **81 ANCHORED / 5 REVIEW / 4 NEEDS_ANCHOR.** The 5 REVIEW (T1SS_01/03, T2SS_03, T3SS_01, T4SS_13) resolved product-tier only but are biologically correct (HlyB/D/TolC, Hxc, Cds, Vbh) -> usable, flagged. The 4 NEEDS_ANCHOR all trace to genome-assignment, NOT missing curation (see Checkpoint A integrity list): T6SS_07 + T6SS_11 need founding-paper locus_tags (genes annotated hypothetical/locus-tag-only); T6SS_20 genome mis-assigned; T2SS_16 fragment genome. 130 product-tier + 175 ambiguous rows carried with flags for later scrutiny.
- [x] 3.7 `data/machinery/machinery_coverage.tsv` + tallies. Curation (3.4): T1 8C/0P, T2 15C/1P, T3 26C/6P, T4 8C/5P, T6 12C/9P = **69 COMPLETE / 21 PARTIAL** (0 REFERENCE_ONLY/NONE_KNOWN). Resolution-grounded anchor (3.6): T1 6A/2R, T2 14A/1R/1N, T3 31A/1R, T4 12A/1R, T6 18A/3N = **81 ANCHORED / 5 REVIEW / 4 NEEDS_ANCHOR**.

## 4. Checkpoint A (user review)

- [x] 4.1 Summarized gold set (582 effectors / 273 effector-paper DOIs / 62 genomes), answer key (90 instances, 1135 genes / 224 machinery DOIs; 81 ANCHORED / 5 REVIEW / 4 NEEDS_ANCHOR), and proposed the data-first panel. Presented as the Checkpoint A doc.
- [x] 4.2 **Teo's Checkpoint A decisions (2026-06-10):** (a) **DROP T6SS_20** (Xanthomonas mis-assignment, no effector rows keyed to it). (b) **DROP T2SS_16** (X12831 fragment genome, no effector rows keyed to it). (c) **Phase 2 panel = ALL 62 genomes** (run ssign on every genome carrying a gold effector). (d) **Freeze gold set at 582** - neither held pool (177 T6SS panel-expansion, 289 Dot/Icm type-IVB) brought in scope. Gold set + answer key approved as-is. Phase 1 unblocked.

**4.0 Effector-locus integrity findings (surfaced while verifying machinery in 3.6; decisions needed before Phase 1).** An integrity check (do each instance's `effector_loci` exist in its assigned RefSeq genome?) flagged 14/90 instances. Root causes:
  - **Underscore/old-tag nomenclature (10 instances: T2SS_11, T3SS_01, T3SS_02-07, T4SS_03, T4SS_04, T4SS_13).** The corpus drops the underscore RefSeq uses: corpus `ECs4550` vs RefSeq `ECs_4550`; `CBU0041` vs `CBU_0041`. Effectors ARE in the genome. **Fix:** Phase 1's effector gene-order index (task 5.1) must normalize locus_tags (insert prefix/digit underscore + case-fold) and match old_locus_tag, exactly like the machinery resolver. No data loss.
  - **Plasmid replicon not cached (1: T3SS_22).** Yersinia enterocolitica pYV effectors (`YE_pYV####`) are on the virulence plasmid, not the cached chromosome. Known edge case (task 6.1). **Fix:** fetch + include the pYV plasmid accession.
  - **Genome mis-assignment (1: T6SS_20).** `A1S_1292` is A. baumannii ATCC 17978 (`NC_009085`, T6SS_11's genome), but the instance is labelled AB307-0294 with genome `NZ_CP012003` (*Xanthomonas citri*). **RESOLVED (Teo 4.2): DROP** the instance. No effector rows are keyed to it.
  - **Fragment genome (1: T2SS_16).** Genome `X12831` is a 1.8 kb EMBL *pul*-operon fragment. **RESOLVED (Teo 4.2): DROP** the instance. No effector rows are keyed to it.

  Items 1 (underscore nomenclature, 10 instances) + 2 (Yersinia pYV plasmid, T3SS_22) stay OPEN, handled inside Phase 1: the effector gene-order index (5.1) normalizes locus_tags + matches old_locus_tag (same alias logic as the machinery resolver), and 6.1 fetches/confirms the pYV accession. Same version/prefix drift affects genome accessions (`NC_002516` vs `NC_002516.2`, `HG326223` vs `NZ_HG326223`) -> Phase 1 must version-strip + prefix-normalize when grouping effectors by genome.

**4.3 T1SS effector rescue + adjacency ceiling (Teo-directed, done 2026-06-11; ahead of Phase 1).** The corpus left 19/28 validated T1SS effectors with no genome (old UniProt entries cite EMBL gene clones, not assemblies). Per Teo: place them into a representative same-species genome (>=90% id after he opted to include the 93% prtA), then -- because T1SS is operonic -- *verify* the transporter is adjacent rather than assume ceiling=100%, so any later ssign miss of a confirmed-adjacent effector is a true ssign false negative, not a structural impossibility.
  - Scripts `12_rescue_t1ss_ipg.py` (IPG identical-protein) + `13_rescue_t1ss_blast.py` (blastp >=90% id/>=90% cov) + `14_finalize_t1ss_rescue.py` (merge) -> **placed 16/19** (9 IPG + 7 BLAST; 13 exact-species, 3 genus-only). 3 unplaceable: prtG (61% divergent), prtA P82115 + lktA P55123 (no RefSeq genome for the exact strain).
  - `15_t1ss_adjacency.py` reads the gene order around each placed effector for the T1SS transporter signature (ABC/HlyB-family + HlyD-family MFP; literature families, read off RefSeq annotation = ssign-independent). `16_t1ss_replace_fragmented.py` re-placed effectors stuck on fragmented contigs into complete genomes (hlyA: a 3 kb single-CDS contig -> NZ_CP031766.1, operon intact).
  - **Result: 14/16 adjacency CONFIRMED** (transporter within 1-3 genes -> reachable at N=3), **2 genuine exceptions** (apxIIA = apxIICA operon has no transporter, trans-secreted by ApxIB/D; serralysin = Lip transporter at a separate locus, confirmed across 13 complete Serratia genomes). The 2 are real "impossible" cases. Tables: `data/t1ss_rescue/{t1ss_rescued.tsv, t1ss_ceiling.tsv}`.
  - **STILL TODO in Phase 1:** fold the 16 placements into `effector_gold_set.tsv` (with `placement_tier` + `species_match` columns), add their placement genomes to the Phase 2 panel, and treat the T1SS ceiling as: 14 reachable + 2 impossible (documented). T1SS testable effectors: 9 -> 25. See NOTES.md "T1SS effector rescue".

## 5. Phase 1: ceiling / impossible analysis

**Phase 1 prep (Teo-directed, done before 5.1):** folded the 16 rescued T1SS placements into `data/phase1/effector_gold_set_phase1.tsv` (`scripts/17_fold_t1ss_rescue.py`): +`ceiling_source` (answer_key | adjacency_scan), `placement_tier`, `species_match`, `testable`/`testable_reason`; 16 placed, 3 flagged untestable. **Net-new instance assignment (Checkpoint-A fork):** 53 net-new SecReT6 effectors split 20 unique-genome (auto-assigned, rigorous) / 25 multi-T6SS-genome / 8 no-curated-instance. **Teo decision: drop the 33 ambiguous to untestable** (no nearest-machinery guessing). pulA (T2SS_16, dropped fragment genome) -> untestable, documented.

- [x] 5.1 Gene-order index: `scripts/bench_index.py` (shared lib: drift-tolerant accession + locus_tag matching, per-replicon ordinals, `find`/`find_by_gene`/`gene_distance`) + `scripts/18_build_gene_order.py` -> `data/phase1/gene_order_index.tsv` (91 replicons, 266,677 CDS). Resolves 86/87 gold genomes (only X12831, the dropped fragment, unresolved). Reusable by Phase 2.
- [x] 5.2 `scripts/19_effector_distance.py`: per effector, gene-order distance to nearest anchored own-instance machinery (answer key); 16 rescued T1SS use the adjacency-scan distance. Effector located by locus_tag, else by **unique gene symbol** (9 placed incl. Yersinia Yops on pYV; flagged `effector_match=gene_symbol`).
- [x] 5.3 Classify reachable/impossible/untestable at N=3/5/7 -> `data/phase1/ceiling_per_effector.tsv`. **499 testable / 83 untestable.**
- [x] 5.4 `scripts/20_aggregate_ceiling.py` -> `ceiling_summary.tsv` + `ceiling_by_genome.tsv`. **Ceiling@3/5/7: T1 80/80/80, T2 1/3/4, T3 21/29/33, T4 6/8/10, T6 22/24/26, ALL 18/23/25 (%).**
- [x] 5.5 `scripts/21_ceiling_figures.py` -> `figures/01_gold_set_composition.png`, `02_ceiling_vs_window.png`, `03_distance_ecdf.png`, `04_per_genome_ceiling.png` (all viewed).
- [x] 5.6 `docs/phase1_ceiling.md` (method, testable/untestable denominator, the biology, strengths, limitations) + README Phase 1 result block + pointer. Simplify pass run over scripts 17-21 + bench_index (shared `load_tsv`, `set_reachable`, `UNPLACEABLE_REASON`; re-ran identical).

## 6. Phase 2: actual recall

- [x] 6.1 `scripts/22_stage_inputs.py` -> `inputs/<unit>.fasta` (67 assemblies; union-find merges accession-split replicons so each run = one full assembly) + `data/phase2/{panel_manifest,effector_unit_map}.tsv`. All 77 needed replicons cached (incl. Yersinia pYV NC_008791.1 + chr bundled; Brucella chr I+II; Vibrio/Salmonella/Shigella plasmids); none unavailable. 568 genome-bearing gold effectors mapped to a unit. Nucleotide FASTA (forces Bakta re-annotation = real user path).
- [x] 6.2 Ran ssign on the full panel (CX3 gpu72, 12 jobs, 2026-06-12): both tags `panel_genbank_default` (T3SS excluded) + `panel_genbank_t3ss` (T3SS included), 67/67 genomes clean, 196,170 protein rows each. Synced to `data/phase2/runs/`. Pilot settled GenBank==FASTA recall so panel ran GenBank-only. **Harness (task 6.1b):** `scripts/cx3/run_benchmark_batch.pbs` (extended tier = DLP+SignalP+DeepSecE+PLM-E ON, annotation off, FASTA in) + `make_batches.py` (6 batches + T3SS pilot) + `README.md`. **T3SS pilot runs first** (NC_003197.2 Salmonella, NC_004337.2 Shigella, NC_004578.1 P.syringae, NC_002516.2 PAO1 control) default vs `--excluded-systems Flagellum Tad` to settle the exclusion question before the full panel. AWAITING Teo's CX3 submit.
- [x] 6.3 Bridge folded into `scripts/24_actual_call.py` (+ `bench_runout.py`). **Method changed from coordinate overlap to sequence/locus identity:** ssign's output CSVs carry NO genomic coordinates, but `results_raw.csv` has a per-protein `sequence` column. So each gold effector matches ssign's protein by drift-normalised locus_tag (exact in GenBank `--use-input-annotations` mode) else exact protein-sequence identity else **>=90% reciprocal identity+coverage** (Teo: guards against Bakta annotating a slightly-shifted ORF start/stop in FASTA mode; difflib matching-blocks, accepted when matched residues >= 90% of BOTH proteins). `match_method` (locus_tag/sequence/sequence_90/unmatched) + `match_identity` columns per effector. Scripts written + validated on a synthetic fixture; awaiting real pilot output.
- [x] 6.4 `scripts/24_actual_call.py --run-tag <tag>` -> `data/phase2/actual_per_effector.<tag>.tsv` (582 rows, one per gold effector; keyed by genome->unit not uniprot, since ~188 rows share an empty uniprot). Call = emitted_secreted / not_emitted / not_in_input / no_run, with the per-tool signals (dlp/dse/signalp/plm-e/nearby_ss_types) carried through to explain misses + double as the secretion-classifier feature set. Synthetic-fixture-tested (locus, sequence, unmatched paths all correct).
- [x] 6.5 `scripts/25_actual_vs_ceiling.py --run-tag <tag>` -> `actual_vs_ceiling.<tag>.tsv` (per SS type: ceiling@3/5/7 + actual + the emitted×reachable@3 2x2). Reproduces the Phase 1 ceiling exactly from the per-effector table (consistency check passed). **Reports** emitted-but-ceiling-unreachable rows rather than hard-asserting actual<=ceiling: ssign's proximity uses MacSyFinder-detected components vs the literature answer-key machinery, so actual>ceiling is a real, interesting signal (ssign detected machinery the answer key didn't anchor), not an error.
- [x] 6.6 Figures (`scripts/27_phase2_figures.py` -> `data/phase2/figures/01..04`, all viewed): 01 recall funnel (testable 499 -> findable@3/5/7 91/113/127 -> found 39/51), 02 per-type ceiling@7 vs actual, 03 emission basis (own-type vs accidental cross-type), 04 discordant-audit verdicts. Plus `scripts/26_found_systems.py` -> `found_systems.*.tsv` (each found effector -> the ssign-detected `sys_id` it sits within +/-3 of; needed a TXSScan-subtype family normalization, `T6SSi`->`T6SS`).
- [x] 6.7 Docs: `data/phase2/RESULTS.md` (full-panel three-number result per type + edge cases) and `data/phase2/discordant_audit.md` (19-effector literature audit: only 5/19 sound; ≥9 wrong/404 DOIs, 2 SS-type misassignments, 4 unidentifiable rows, 1 duplicate). Citation-repair list logged in NOTES.

## 6b. T5SS exploratory side study (preliminary, no ground-truth claim)

- [ ] 6b.1 From the Phase 2 runs, extract the proteins ssign flags as secreted within the proximity window of detected T5aSS clusters.
- [ ] 6b.2 Literature-check a sample: are any of these neighbors recorded as T5aSS-associated secreted proteins, or are they the autotransporter itself / unrelated neighbors? Record DOIs and quotes.
- [ ] 6b.3 Write a short, clearly-labelled preliminary note (separate from the headline results) summarizing the observation and what, if anything, the literature supports.

## 7. Wrap-up

- [ ] 7.1 Update `validation_sweeps/README.md` to point at the new benchmark section.
- [ ] 7.2 Record deferred items (ssign whole-genome-flag bug, any external DB that stayed unreachable, the T5aSS-neighbor follow-up) in the repo's NOTES/backlog with triggers.
- [ ] 7.3 Run the simplify pass over the new analysis scripts; verify reproducibility from a clean checkout.
